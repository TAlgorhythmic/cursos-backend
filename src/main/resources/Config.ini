<HttpServerSection>
httpHost=localhost
;; Uncomment to edit the port. If not uncommented, the default port will be used (8080)
;;httpPort=8080

<DatabaseSection>
dbHost=localhost
database=cursos
username=username
password=secretpassword

;; Uncomment to edit the port. If not uncommented, the default port will be used (3306)
;;dbPort=3306